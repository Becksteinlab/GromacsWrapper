#codecov:
#  branch: main

coverage:
  status:
    project: true
      #default: false
      # tests:
      #   paths: ["tests/"]
      #   target: 100%
      # package:
      #   paths: ["gromacs/"]
    patch: true
      # default: false
      # test:
      #   paths: ["tests/"]
      #   target: 100%
      #   threshold: 0%
      # package: 
      #   paths: ["gromacs/"]
      #   target: auto
      #   threshold: 0%
